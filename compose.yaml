services:
  web:
    build: .
    ports:
      - "3333:3000"
    depends_on:
      - db

  db:
    build:
      dockerfile: Dockerfile.mysql
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: